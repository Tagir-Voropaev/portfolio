import React from 'react'
import s from './ReposSection.module.scss'
const ReposSection = () => {
  return (
    <section className={s.repos}>
        <h2 className={s.title}>Код</h2>
        <ul className={s.reposList}>
          <li className={s.reposItem}>
            
          </li>
        </ul>
    </section>
  )
}

export default ReposSection